import streamlit as st

#st.write("something")

with (st.container(border = True, horizontal = True)):

    logo = "Logo"
    name = "Weathercast"
    search = "search"

    st.write(logo,name,search)
